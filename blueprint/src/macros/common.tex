% Shared macros for both versions.  NOTE: the \newtheorem definitions
% live directly in web.tex and print.tex (plasTeX quirk: theorem
% environments defined inside an \input file lose their labels and
% \lean/\uses metadata in the dependency graph).
